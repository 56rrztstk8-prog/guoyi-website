---
import BaseLayout from "../layouts/BaseLayout.astro";
import { site } from "../data/site";

// Homepage hero image path
const heroImage = import.meta.env.BASE_URL + "images/hero.jpg";
---

<BaseLayout title={undefined} description="Guo Yi — Artist Website" menuMode="hover">
  <style>
    .hero {
      position: fixed;
      inset: 0;
      z-index: 0;
    }

    .hero img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .signature {
      position: fixed;
      top: 1.5rem;
      left: var(--menu-trigger-width);
      padding-left: 1.2rem;
      z-index: 50;
      font-size: var(--font-size-name);
      letter-spacing: 0.25em;
      color: #fff;
    }
  </style>

  <div class="hero">
    <img src={heroImage} alt="" />
  </div>

  <span class="signature">{site.name}</span>
</BaseLayout>
